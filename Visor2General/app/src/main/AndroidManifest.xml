<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.visor2general">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/lab"
        android:label="@string/app_name"
        android:roundIcon="@drawable/lab"
        android:supportsRtl="true"
        android:theme="@style/Theme.Visor2General">
        <activity
            android:name=".Video"
            android:label="@string/title_activity_video"
            android:theme="@style/Theme.Visor2General.NoActionBar"></activity>
        <activity
            android:name=".Foto"
            android:label="@string/title_activity_foto"
            android:theme="@style/Theme.Visor2General.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>